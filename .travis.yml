language: python
sudo: required
dist: xenial
python:
  - 3.5
  - 3.6
  - 3.7
install:
  - pip install -r base_requirements.txt
  - pip install -U pytest==4.3.0 pytest-cov coveralls flake8
  - pip install sqlalchemy
script:
  - flake8 .
  - python -m pytest --cov .
after_success:
  - coveralls
